import React from 'react'

const ForumCategory = props => {
  return (
    <div>
      <div class={props.category}>
        {/*
          similar structure to the forum
          probably want to make left and right panes to switch between
        */}
      </div>
    </div>
  )
}

export default ForumCategory
