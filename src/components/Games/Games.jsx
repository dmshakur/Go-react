import React from 'react'

const Games = props => {
  return (
    <div>
      {/*
        Will loop through all the active and available games showing live displayName
        games will show three max horizontally with pagination
      */}
    </div>
  )
}

export default Games
